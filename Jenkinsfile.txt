pipeline {
    agent { docker 'maven:3.5-alpine' }
    stages {
        stage ('Checkout') {
          steps {
            git 'https://github.com/Liner246/Web_serv.git'
          }
        }
        stage('Build') {
            steps {
                sh 'docker build -t nginx .'
                
            }
        }
    }
}